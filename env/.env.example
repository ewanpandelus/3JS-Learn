# Copy to "env/.env.local" (gitignored) and fill with values from the Supabase dashboard
# (Settings → API: Project URL and anon public key). Regenerate the anon key if it was
# ever committed to git — see https://github.com/ewanpandelus/3JS-Learn/issues/1

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
